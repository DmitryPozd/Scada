# Решение проблем SCADA приложения

## Проблема 1: Катушки не включаются при нажатии ON/OFF

### Симптомы
- Кнопки ON/OFF на CoilButton и ImageButton не включают катушки контроллера
- В строке состояния показывается: "⚠ НЕТ включённых Coil тегов!"
- Ранее всё работало корректно

### Причина
Файл `settings.json` в `%AppData%\Scada.Client\` содержит **пустой или устаревший список тегов**.

**Последовательность событий:**
1. При первом запуске приложение создаёт `settings.json` с пустым `Tags: []`
2. Теги из `tags.json` (196K тегов) НЕ загружаются, потому что settings уже существует
3. Без тегов типа `RegisterType.Coils` система не может опрашивать состояние катушек
4. Запись команд работает, но визуальная обратная связь отсутствует

## Проблема 2: Программа зависает при открытии диалога смены тега ⚠️ FIXED!

### Симптомы
- Правый клик на кнопке → диалог не открывается или зависает
- Приложение "не отвечает" на несколько секунд
- UI замораживается при попытке выбора тега

### Причина
Попытка загрузить **все** Coil-теги (~196000 шт.) в ComboBox одновременно:
- Создание ObservableCollection из всех тегов
- Проверка адресов каждого тега (>= 1536) вызывала перебор всех тегов
- Генерация UI элементов для каждого тега
- Нехватка памяти и процессорного времени

### Решение ✅ Исправлено!
1. **Удалена проверка адресов:** Убрана фильтрация `t.Address >= 1536` (она вызывала зависание)
2. **Только фильтр по типу:** Проверяется только `RegisterType.Coils` (быстрая операция)
3. **Лимит загрузки:** Берутся только первые 500 тегов из коллекции
4. **Лимит отображения:** Показывается max 200 тегов без фильтра
5. **Быстрый поиск:** При вводе фильтруется в памяти, показывается max 200 результатов

**Производительность:**
- Открытие диалога: **< 100ms** (было 5-10 секунд)
- Поиск по имени/адресу: **мгновенно**

## Проблема 3: Назначены неправильные теги (X-входы вместо M/Y) ⚠️ NEW!

### Симптомы
- Кнопка не включает катушку даже после "успешной" записи
- В Debug-логах ошибки: "Illegal data address" или "Slave device failure"
- Контроллер игнорирует команды записи

### Причина
**X-теги (адреса 0-1535) — это дискретные ВХОДЫ контроллера:**
- Предназначены только для ЧТЕНИЯ состояния (кнопки, датчики)
- Запись в X-теги технически невозможна или игнорируется контроллером
- Modbus FC05/FC15 (Write Coil) не работает для X-адресов

### Решение ✅ Исправлено!
**Используйте только записываемые теги:**
- **Y-теги (1536+):** Дискретные выходы (реле, контакторы)
- **M-теги (3072+):** Внутренние флаги (логические переменные)

**Адреса по умолчанию изменены:**
```
Старые (неправильные):        Новые (правильные):
Coil1: 0    (X0 - read-only)  Coil1: 3072 (M0 - writable)
Coil2: 1    (X1 - read-only)  Coil2: 3073 (M1 - writable)
Coil3: 2    (X2 - read-only)  Coil3: 3074 (M2 - writable)
Motor: 10   (X10 - read-only) Motor: 3075 (M3 - writable)
Valve: 11   (X11 - read-only) Valve: 3076 (M4 - writable)
Fan: 12     (X12 - read-only) Fan: 3077   (M5 - writable)
```

**Диалог выбора тега теперь автоматически:**
- ✅ Показывает только Y и M теги
- ❌ Скрывает X-теги (с предупреждением в UI)
- 📝 Подсказка: "только Y-выходы и M-флаги, X-входы нельзя записывать"

### Быстрое решение

#### Вариант 1: PowerShell скрипт (рекомендуется)
```powershell
# Запустите в корне проекта
.\fix-tags-issue.ps1
```

#### Вариант 2: Ручное удаление
1. Откройте PowerShell
2. Выполните команду:
   ```powershell
   Remove-Item "$env:APPDATA\Scada.Client\settings.json" -ErrorAction SilentlyContinue
   ```
3. Перезапустите приложение:
   ```powershell
   dotnet run --project Scada.Client
   ```

### Что было исправлено в коде

**Изменения в `MainWindowViewModel.cs`:**

1. **Новый порядок инициализации:**
   - Создан метод `InitializeAsync()` для последовательной загрузки:
     1. `settings.json` (пользовательские настройки)
     2. `tags.json` (дополняет отсутствующие теги)
     3. `InitializeDefaultTags()` (только если всё пусто)

2. **Автосохранение после загрузки:**
   - После загрузки тегов из `tags.json` они сохраняются в `settings.json`
   - Следующий запуск не требует повторной загрузки

3. **Улучшенная диагностика:**
   - Debug-логи показывают количество загруженных тегов на каждом этапе
   - Строка состояния показывает детальную информацию об опросе

### Проверка работы

После перезапуска приложения:

1. **Подключитесь к контроллеру** (кнопка "Подключить")
2. **Проверьте строку состояния:**
   - Должна появиться: "Найдено N включённых Coil тегов (адреса: ...)"
   - Если показывает "⚠ НЕТ включённых Coil тегов!" — см. раздел "Дополнительная диагностика"

3. **Проверьте Debug-логи** (в IDE или консоли):
   ```
   Settings loaded: N tags in ConnectionConfig
   Loaded N tags from tags.json into ConnectionConfig
   ```

### Дополнительная диагностика

#### Проверка файла settings.json
```powershell
# Показать путь к файлу
Write-Host "$env:APPDATA\Scada.Client\settings.json"

# Показать количество тегов
(Get-Content "$env:APPDATA\Scada.Client\settings.json" | ConvertFrom-Json).Tags.Count
```

#### Проверка файла tags.json
```powershell
# В каталоге приложения (bin/Debug/net8.0/)
(Get-Content "Scada.Client/bin/Debug/net8.0/tags.json" | ConvertFrom-Json).Tags.Count
```

#### Включение подробных логов
В `MainWindowViewModel.cs` уже добавлены `System.Diagnostics.Debug.WriteLine(...)` — смотрите Output окно в IDE.

### Предотвращение проблемы в будущем

**Рекомендации:**
1. Используйте **кнопку "Настройки"** для редактирования тегов (когда будет реализована)
2. Не редактируйте `settings.json` вручную — используйте UI приложения
3. Резервное копирование `settings.json` перед экспериментами:
   ```powershell
   Copy-Item "$env:APPDATA\Scada.Client\settings.json" "$env:APPDATA\Scada.Client\settings.backup.json"
   ```

### Связанные файлы

- `Scada.Client/ViewModels/MainWindowViewModel.cs` — основная логика загрузки тегов
- `Scada.Client/Services/SettingsService.cs` — сохранение/загрузка settings.json
- `Scada.Client/Services/TagsConfigService.cs` — загрузка tags.json
- `Scada.Client/tags.json` — база тегов (196K+ записей)
- `%AppData%\Scada.Client\settings.json` — пользовательские настройки

### Контакты и помощь

Если проблема сохраняется после всех шагов:
1. Проверьте Debug-логи приложения
2. Запустите `fix-tags-issue.ps1` с выводом диагностики
3. Создайте issue с логами и описанием проблемы
