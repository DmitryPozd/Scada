# Руководство по редактору активных тэгов

## Обзор

**Редактор активных тэгов** (`Settings` → `Редактор тегов`) позволяет точно контролировать, какие регистры будут опрашиваться приложением. Это значительно повышает производительность и надёжность связи с контроллером.

## Интерфейс

Окно разделено на **две панели**:

### Левая панель: Доступные тэги
- **Поиск тэгов**: введите имя (например, `Y10`) или адрес (например, `1546`) для поиска в полном списке из `tags.json`
- **Быстрое добавление диапазона**: введите диапазон в формате `Y0-Y50`, `M100-M200`, `V512-V600` для массового добавления
- **Таблица доступных тэгов**: показывает тэги из `tags.json`, которые ещё не добавлены в активные

### Правая панель: Активные тэги
- **Список опрашиваемых тэгов**: только эти тэги сохраняются в `settings.json` и опрашиваются
- **Редактирование параметров**: можно изменить `Масштаб`, `Смещение`, `Включен`
- **Удаление**: кнопка `❌ Удалить выбранный` возвращает тэг обратно в доступные

## Основные операции

### 1. Поиск и добавление отдельных тэгов

**Шаги:**
1. В поле **"Поиск тегов"** введите имя или адрес (например, `Y10` или `1546`)
2. Нажмите **🔍** для поиска
3. Выберите нужный тэг в **левой таблице**
4. Нажмите **➡️ Добавить в активные**
5. Тэг появится в **правой таблице**

### 2. Добавление диапазона тэгов

**Формат диапазона**: `<Префикс><НачальныйНомер>-<Префикс><КонечныйНомер>`

**Примеры:**
- `Y0-Y50` — добавит выходы Y0, Y1, Y2, ..., Y50 (51 тэг)
- `M100-M200` — добавит внутренние регистры M100...M200 (101 тэг)
- `V512-V600` — добавит V-регистры V512...V600 (89 тэгов)
- `AI0-AI15` — добавит аналоговые входы AI0...AI15 (16 тэгов)

**Шаги:**
1. В поле **"Быстрое добавление диапазона"** введите диапазон (например, `Y0-Y50`)
2. Нажмите **➕**
3. Все тэги из диапазона добавятся в **правую таблицу**

**Примечания:**
- Префиксы должны совпадать: ❌ `Y0-M50` (неправильно), ✅ `Y0-Y50` (правильно)
- Начальный номер ≤ конечного: ❌ `Y50-Y0`, ✅ `Y0-Y50`
- Добавляются только тэги, которых ещё нет в активных

### 3. Удаление тэгов

**Шаги:**
1. Выберите тэг в **правой таблице** (Активные тэги)
2. Нажмите **❌ Удалить выбранный**
3. Тэг вернётся в левую панель (Доступные)

### 4. Редактирование параметров

В **правой таблице** можно изменить:
- **Масштаб** (`Scale`) — коэффициент умножения значения
- **Смещение** (`Offset`) — добавляется к значению после умножения
- **Включен** (`Enabled`) — галочка для временного отключения опроса

**Формула**: `Отображаемое значение = (Сырое значение × Масштаб) + Смещение`

**Пример**: Температурный датчик 0-100°C → модуль АЦП возвращает 0-1000:
- Scale = `0.1`
- Offset = `0`
- Результат: `1000 × 0.1 + 0 = 100°C`

### 5. Сохранение изменений

**Шаги:**
1. После настройки активных тэгов нажмите **💾 Сохранить**
2. Окно закроется
3. Изменения сохранятся в `%AppData%\Scada.Client\settings.json`
4. **Переподключитесь к контроллеру** для применения новой конфигурации опроса

## Преимущества нового подхода

### До изменений:
- ❌ Загружались **первые 20 тэгов каждого типа** (X, Y, M, AI, AQ, V и т.д.)
- ❌ Опрашивались **все загруженные тэги**, даже неиспользуемые
- ❌ Большие диапазоны адресов вызывали ошибки "address not allowable"
- ❌ Невозможно было выбрать конкретные тэги (например, только Y10, Y20, Y30)

### После изменений:
- ✅ В `settings.json` сохраняются **только выбранные тэги**
- ✅ Опрашиваются **только активные тэги** (минимальный трафик Modbus)
- ✅ Пользователь контролирует **какие именно регистры существуют** в контроллере
- ✅ Быстрое добавление диапазонов (например, `Y0-Y1023` одной командой)
- ✅ Поиск по имени/адресу среди **35,421 тэгов** из `tags.json`

## Рекомендации

### Для начала работы:
1. **Определите используемые регистры** в вашей программе ПЛК
2. **Добавьте только эти тэги** через редактор (поиск или диапазон)
3. **Включите только нужные** (галочка `Enabled`)
4. **Сохраните** и переподключитесь

### Для Haiwell PLC:
- **Входы X**: обычно X0-X31 (в зависимости от модели)
- **Выходы Y**: обычно Y0-Y15 или Y0-Y31
- **Внутренние M**: используйте только те, что реально заданы в программе
- **Аналоговые AI/AQ**: только те каналы, где подключены датчики/актуаторы
- **V-регистры**: добавляйте по мере необходимости (не все 15,000)

### Оптимизация производительности:
- **Меньше тэгов** = быстрее опрос = меньше нагрузка на PLC
- **Группируйте адреса**: Y0-Y15 читается одним запросом, Y0+Y100 — двумя
- **Отключайте неиспользуемые**: снимите галочку `Enabled` вместо удаления

## Пример использования

### Задача: 
Контроллер управляет **8 насосами** (Y0-Y7) и **3 клапанами** (Y10-Y12). Есть **4 датчика температуры** (AI0-AI3).

### Действия:
1. Открыть `Settings` → `Редактор тегов`
2. В поле диапазона ввести `Y0-Y7` → нажать **➕**
3. В поле диапазона ввести `Y10-Y12` → нажать **➕**
4. В поле диапазона ввести `AI0-AI3` → нажать **➕**
5. Нажать **💾 Сохранить**

### Результат:
- В `settings.json`: **15 тэгов** вместо 100+
- Опрос занимает **~50 мс** вместо **~500 мс**
- Нет ошибок "address not allowable" (не опрашиваются несуществующие V0-V1000)

## Устранение проблем

### Тэг не появляется при добавлении диапазона
- **Причина**: тэга нет в `tags.json`
- **Решение**: проверьте правильность имени, используйте **поиск** для проверки наличия

### Ошибка "Invalid range format"
- **Причина**: неправильный формат диапазона
- **Правильный формат**: `Y0-Y50` (без пробелов, одинаковые префиксы)

### Опрос не работает после сохранения
- **Причина**: не переподключились к контроллеру
- **Решение**: нажмите `Отключить` → `Подключить` в главном окне

### "Protocol identifier is invalid" после изменений
- **Причина**: соединение разорвалось
- **Решение**: приложение автоматически переподключится (см. статус)

---

**Версия**: 2025-11-03  
**Автор**: GitHub Copilot + пользователь
