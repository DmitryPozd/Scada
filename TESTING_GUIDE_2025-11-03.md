# Инструкция для тестирования редактора активных тэгов

## Дата: 3 ноября 2025 г.

## ✅ Что было сделано

1. **Создан новый редактор тэгов** (`Settings` → `Редактор тегов`)
2. **Двухпанельный интерфейс**:
   - Левая панель: поиск и доступные тэги из `tags.json`
   - Правая панель: активные (опрашиваемые) тэги
3. **Функция быстрого добавления диапазона**: `Y0-Y50`, `M100-M200`
4. **Оптимизация опроса**: только выбранные тэги сохраняются и опрашиваются
5. **Автоматическое переподключение** при ошибке "protocol identifier is invalid"

## 🧪 Сценарии тестирования

### Сценарий 1: Первый запуск с новой конфигурацией

**Цель**: проверить добавление тэгов через редактор

**Шаги**:
1. Запустите приложение
2. Откройте `Settings` → `Редактор тегов`
3. В поле **"Быстрое добавление диапазона"** введите: `Y0-Y10`
4. Нажмите **➕**
5. **Ожидаемый результат**: в правой таблице появятся 11 тэгов (Y0...Y10)
6. Нажмите **💾 Сохранить**
7. Закройте окно настроек
8. **Проверьте** `%AppData%\Scada.Client\settings.json` - должны быть только Y0-Y10

### Сценарий 2: Поиск конкретного тэга

**Цель**: проверить поиск по имени и адресу

**Шаги**:
1. Откройте редактор тэгов
2. В поле **"Поиск тегов"** введите: `Y15`
3. Нажмите **🔍**
4. **Ожидаемый результат**: в левой таблице Y15 (адрес 1551)
5. Выберите Y15
6. Нажмите **➡️ Добавить в активные**
7. **Проверьте**: Y15 переместился в правую таблицу
8. **Попробуйте поиск по адресу**: `1551` → должен найти Y15 (если он ещё в доступных)

### Сценарий 3: Добавление нескольких диапазонов

**Цель**: проверить массовое добавление разных типов

**Шаги**:
1. Очистите активные тэги (удалите все через **❌**)
2. Добавьте диапазоны:
   - `Y0-Y7` (8 выходов)
   - `M100-M110` (11 внутренних регистров)
   - `AI0-AI3` (4 аналоговых входа)
3. **Ожидаемый результат**: в правой таблице 8+11+4 = 23 тэга
4. Сохраните
5. **Проверьте settings.json**: должно быть ровно 23 тэга

### Сценарий 4: Проверка опроса только активных тэгов

**Цель**: убедиться, что опрашиваются только выбранные

**Подготовка**:
1. Откройте редактор, удалите все активные тэги
2. Добавьте **только** `Y0-Y5` (6 тэгов)
3. Сохраните

**Тест**:
1. Подключитесь к контроллеру
2. Откройте **Debug Console** (View → Output → Debug Console)
3. **Проверьте логи**: должны опрашиваться только Y0-Y5 (адреса 1536-1541)
4. **Не должно быть**: запросов к M, V, AI или другим тэгам

### Сценарий 5: Редактирование параметров тэга

**Цель**: проверить изменение Scale/Offset/Enabled

**Шаги**:
1. В правой таблице (активные тэги) выберите любой тэг типа `AI` (например AI0)
2. В столбце **Масштаб** измените на `0.1`
3. В столбце **Смещение** измените на `20`
4. Сохраните
5. **Проверьте settings.json**: AI0 должен иметь `"Scale": 0.1, "Offset": 20`
6. Подключитесь и опросите AI0
7. **Ожидаемый результат**: если контроллер вернул 100, в UI должно быть `100 × 0.1 + 20 = 30`

### Сценарий 6: Отключение тэга без удаления

**Цель**: проверить флаг Enabled

**Шаги**:
1. В правой таблице снимите галочку **Включен** у Y0
2. Сохраните
3. Подключитесь к контроллеру
4. **Проверьте логи**: Y0 не должен опрашиваться
5. Вернитесь в редактор, поставьте галочку обратно
6. Переподключитесь
7. **Проверьте**: Y0 снова опрашивается

### Сценарий 7: Автоматическое переподключение

**Цель**: проверить обработку ошибки "protocol identifier is invalid"

**Подготовка**:
1. Подключитесь к контроллеру
2. Запустите опрос (должны быть активные тэги)

**Тест**:
1. **Во время опроса** выключите контроллер или отключите сеть
2. **Ожидаемый результат**:
   - Статус: `⚠️ Ошибка протокола, переподключение...`
   - Через 1 сек попытка переподключения
   - Если контроллер всё ещё недоступен: `❌ Ошибка переподключения: ...`
3. Включите контроллер обратно
4. **Проверьте**: приложение автоматически переподключится при следующей попытке

### Сценарий 8: Неправильный формат диапазона

**Цель**: проверить валидацию ввода

**Попробуйте неправильные форматы**:
- `Y0-M10` (разные префиксы) → Debug Console: "Tag prefixes must match"
- `Y50-Y10` (начало > конца) → "Start number must be <= end number"
- `Y0` (не диапазон) → "Invalid range format"
- `ABC-DEF` (не числа) → "Failed to parse tag names"

**Ожидаемый результат**: тэги не добавятся, в Debug Console появятся сообщения об ошибках

## 🐛 Что проверить особенно внимательно

### Критические моменты:
1. **settings.json сохраняется корректно**: после закрытия и перезапуска приложения активные тэги должны загрузиться
2. **Опрашиваются только активные тэги**: не должно быть запросов к тэгам, которых нет в правой таблице редактора
3. **Привязки кнопок к тэгам сохраняются**: если назначили Y5 на ImageButton, после перезапуска должно остаться Y5
4. **Переподключение работает**: при потере связи приложение не падает, а пытается переподключиться

### Производительность:
- С 10 тэгами опрос должен занимать **~50-100 мс**
- С 100 тэгами - **~300-500 мс**
- С 1000+ тэгами - **~5-10 сек** (не рекомендуется!)

## 📝 Что записать в результаты

### Для каждого сценария:
- ✅ **Пройден** / ❌ **Провален** / ⚠️ **С замечаниями**
- **Комментарий**: что сработало не так, как ожидалось
- **Скриншоты** (если есть визуальные проблемы)

### Общие вопросы:
1. Понятен ли интерфейс редактора?
2. Удобно ли добавлять диапазоны?
3. Есть ли предложения по улучшению?

## 🚀 После успешного тестирования

### Рекомендуемая конфигурация для вашего ПЛК:
1. Откройте редактор
2. Удалите все автоматически загруженные тэги
3. Добавьте диапазоны:
   ```
   Y0-Y15       (реальные выходы вашего контроллера)
   M0-M50       (используемые в программе)
   AI0-AI7      (подключённые датчики)
   ```
4. Отключите ненужные тэги (снимите **Включен**)
5. Сохраните

### Проверьте производительность:
- Время опроса должно снизиться в 5-10 раз
- Ошибки "address not allowable" должны исчезнуть

---

**Успешного тестирования! 🎉**

Если найдёте баги или будут предложения - сообщите.
